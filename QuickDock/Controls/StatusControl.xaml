<UserControl x:Class="QuickDock.Controls.StatusControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Cursor="Hand">
    <UserControl.Resources>
        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style x:Key="ResourceLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style x:Key="ResourceValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>
    </UserControl.Resources>
    
    <Border x:Name="RootBorder" Background="{StaticResource WindowPanelBrush}" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1"
            Padding="10,3,6,3"
            CornerRadius="4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="WeatherIconText"
                       Grid.Column="0"
                       Text="{Binding WeatherIcon}"
                       FontSize="14"
                       VerticalAlignment="Center"
                       Margin="0,0,4,0"
                       Foreground="{StaticResource TextBrush}"
                       MouseLeftButtonUp="OnRefreshClick"
                       ToolTip="点击刷新天气"/>
            
            <TextBlock Grid.Column="1"
                       Text="{Binding WeatherText}"
                       Style="{StaticResource StatusTextStyle}"
                       Margin="0,0,8,0"
                       MouseLeftButtonUp="OnRefreshClick"/>
            
            <Border Grid.Column="2"
                    Width="1"
                    Background="{StaticResource BorderBrush}"
                    Margin="0,2,6,2"/>
            
            <Grid Grid.Column="4"
                  VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="32"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="CpuLabelText" Grid.Row="0" Grid.Column="0"
                           Text="CPU"
                           Style="{StaticResource ResourceLabelStyle}"/>
                <TextBlock x:Name="MemLabelText" Grid.Row="0" Grid.Column="1"
                           Text="MEM"
                           Style="{StaticResource ResourceLabelStyle}"/>
                <TextBlock x:Name="GpuLabelText" Grid.Row="0" Grid.Column="2"
                           Text="GPU"
                           Style="{StaticResource ResourceLabelStyle}"/>
                
                <TextBlock Grid.Row="1" Grid.Column="0"
                           Style="{StaticResource ResourceValueStyle}">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0}%">
                            <Binding Path="CpuPercent"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock Grid.Row="1" Grid.Column="1"
                           Style="{StaticResource ResourceValueStyle}">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0}%">
                            <Binding Path="MemPercent"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock Grid.Row="1" Grid.Column="2"
                           Style="{StaticResource ResourceValueStyle}">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0}%">
                            <Binding Path="GpuPercent"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </Grid>
        </Grid>
    </Border>
</UserControl>
